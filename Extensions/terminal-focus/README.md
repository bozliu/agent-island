# terminal-focus

VS Code / Cursor compatible extension scaffold for Agent Island terminal focusing.

The OSS rebuild does not yet ship the packaged extension binary. This folder reserves the public integration surface and documents the intended scope:

- identify the currently focused terminal tab
- forward session context to `agent-island-bridge`
- support jump-back from the island into the matching terminal surface
